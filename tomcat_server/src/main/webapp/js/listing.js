// Show the user box, so he can select the pokemon to offer
 function showBox(id) {
    $(id).style.display = 'block';
}
// Hide the user box
function hideBox(id) {
    $(id).style.display = 'none';
}